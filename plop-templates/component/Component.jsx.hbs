import React from 'react';
import styles from './{{pascalCase name}}.module.scss';

const {{pascalCase name}} = ({ className = '' }) => {
  return <div className={`${styles.root} ${className}`}>{`{{pascalCase name}}`}</div>;
};

export default {{pascalCase name}};
